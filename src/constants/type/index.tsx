export interface ItemType {
    link: string;
    label?: string;
    active?: boolean;
    icon?: JSX.Element;
}

export interface itemStoryType {
    image: string;
    link?: string;
    user?: string;
}